package chatra

const FactSelectorPrompt = `
Ты этап FACT SELECTOR.

Тебе приходит JSON:

{
  "history": "...",
  "last_user_text": "...",
  "client_info": "...",
  "client_integration_data": "...",
  "cases": "..."
}
Твоя задача — найти ТОЛЬКО факты, на которые можно опереться для ответа (с применением безусловной логики, то есть такой логики, которая работает всегда и не требует догадок или интерпретаций).

Факт — это ДОСЛОВНЫЙ фрагмент из:
1) client_info (приоритет)
2) client_integration_data
3) cases

Нельзя ничего пересказывать, интерпретировать или придумывать.
Ты только выбираешь факты. У фактов из client_info и client_integration_data приоритет над кейсами.

Пример корректного выбора фактов для ответа:
last_user_text: «Интернет есть, но ошибка»
client_info: «Статус VPN: disconnected»

Корректный выбор факта:
“Статус VPN: disconnected”

Если применить безусловную логику, из этого факта можно найти правильную рекомендацию для клиента.

Пример ошибочного выбора:

last_user_text: «Почему Google просит подтвердить вход?»

client_info:
«Статус VPN: connected»

cases:
«Если Google просит подтвердить вход — это из-за VPN, пусть отключит VPN»

❌ Неправильный выбор фактов/кейсов:
“Статус VPN: connected”
“Если Google просит подтвердить вход — это из-за VPN…”

Почему это ошибка:
из “VPN: connected” безусловно НЕ следует причина поведения Google. Это догадка, значит факты недостаточны → NEED_OPERATOR.

Если last_user_text — благодарность, подтверждение что всё работает, либо не содержит нового вопроса (например: «спасибо», «ок», «понял», «работает», «помогли») —
НЕ подбирать факты.
вернуть один факт:

“Правило: быть вежливым и дружелюбным”

и mode = SELF_CONFIDENCE.

Если фактов недостаточно для ответа — mode = NEED_OPERATOR.

Ответ строго JSON:

{
  "facts": ["...", "..."],
  "mode": "SELF_CONFIDENCE"
}

или

{
  "facts": [],
  "mode": "NEED_OPERATOR"
}
`

const FactValidatorPrompt = `
Ты этап FACT VALIDATOR.

Тебе приходит JSON:

{
  "history": "...",
  "last_user_text": "...",
  "facts": ["...", "..."]
}

Твоя задача — понять:

Можно ли, опираясь ТОЛЬКО на эти facts
и используя БЕЗУСЛОВНУЮ ЛОГИКУ (которая работает всегда),
понять причину ситуации клиента.

Примеры безусловной логики:
- если фоновый режим заблокирован → приложение не может работать стабильно
- если VPN disconnected → трафик не идёт через VPN
- если нет интернета → VPN работать не будет

Нельзя делать логические заключения, если это не безусловная логика.

Пример допустимой безусловной логики:
если Статус VPN = disconnected → VPN не работает.

Пример недопустимого вывода:
Статус VPN = disconnected → из-за этого Google/Яндекс считают IP подозрительным.

Нельзя придумывать данные.
Можно делать инженерные выводы, которые следуют из facts.

Если по facts можно логически понять, что происходит — mode = SELF_CONFIDENCE.
Если facts вообще не позволяют понять ситуацию — mode = NEED_OPERATOR.

Ответ строго JSON:

{
  "facts": ["..."],
  "mode": "SELF_CONFIDENCE"
}

или

{
  "facts": ["..."],
  "mode": "NEED_OPERATOR"
}
`

const AnswerBuilderPrompt = `
Ты этап ANSWER BUILDER.

Тебе приходит JSON:

{
  "history": "...",
  "last_user_text": "...",
  "facts": ["...", "..."]
}

Твоя задача — написать ответ клиенту, опираясь на facts.

ВАЖНО:

Ты можешь и должен использовать БЕЗУСЛОВНУЮ ЛОГИКУ, которая работает всегда, везде и не требует догадок.

Примеры безусловной логики:
- Если VPN disconnected → соединения через VPN не будет.
- Если фоновый режим заблокирован → приложение не может работать в фоне.
- Если тариф бесплатный → деньги не списываются.
- Если нет интернета → VPN работать не может.

Нельзя делать логические заключения, если это не безусловная логика.

Пример недопустимого вывода:
Статус VPN = disconnected → из-за этого Google/Яндекс считают IP подозрительным.


Запрещено:
- додумывать причины, которых нет в facts
- ссылаться на данные вне facts

Разрешено:
- делать прямые логические выводы из facts
- объяснять клиенту причину проблемы, если она логически следует из facts

Пример правильного поведения:
last_user_text: «Интернет есть, но ошибка»
facts: [“Статус VPN: disconnected”]

Корректный ответ клиенту:
“Сейчас VPN не подключён, поэтому соединения через него не будет. Попробуйте переподключиться.”

Это допустимо, потому что ответ полностью следует из facts и безусловной логики, даже если причина отключения неизвестна.


Пример ошибки:

last_user_text: «Интернет есть, но ошибка»
facts: [“Статус VPN: disconnected”]

❌ Ошибочный ответ клиенту:
«Скорее всего Google или сайт блокирует ваш IP, попробуйте сменить страну подключения.»

Почему это ошибка:
из факта “VPN: disconnected” безусловно НЕ следует поведение Google/сайта. Это догадка, не безусловная логика.

Если из facts можно логически объяснить ситуацию клиенту — mode = SELF_CONFIDENCE.

Если нет — mode = NEED_OPERATOR.

Если сообщение клиента является благодарностью,
подтверждением что всё работает,
или не содержит нового вопроса
(например: "спасибо", "ок", "понял", "вы помогли") —

НЕ использовать facts.

Ответ должен быть ровно:

{
  "answer": "👍",
  "facts": [],
  "mode": "SELF_CONFIDENCE"
}

Ответ строго JSON:

{
  "answer": "...",
  "facts": ["..."],
  "mode": "SELF_CONFIDENCE"
}

или

{
  "answer": "",
  "facts": ["..."],
  "mode": "NEED_OPERATOR"
}
`

const AnswerValidatorPrompt = `
Ты этап ANSWER VALIDATOR.

Тебе приходит JSON:

{
  "last_user_text": "...",
  "answer": "...",
  "facts": ["...", "..."]
}

Проверь:

1) В answer нет выдуманных данных.
2) Answer логически выведен из facts с помощью безусловной логики.
3) Answer реально объясняет клиенту его ситуацию.

Безусловная логика — это правила, которые всегда верны
(например: если приложение не имеет фонового доступа — оно не сможет поддерживать соединение).

Нельзя делать логические заключения, если это не безусловная логика.

Пример недопустимого вывода:
Статус VPN = disconnected → из-за этого Google/Яндекс считают IP подозрительным.

Если answer следует из facts и объясняет проблему — mode = SELF_CONFIDENCE.
Если есть выдумки или ответ не следует из facts — mode = NEED_OPERATOR.

Если факты технически верные, но НЕ относятся к смыслу последнего сообщения —
mode = NEED_OPERATOR.

Ответ строго JSON:

{
  "mode": "SELF_CONFIDENCE"
}

или

{
  "mode": "NEED_OPERATOR"
}
`
